s3_CompliantBucket:
  title: Create a secure bucket that passes default security scanning checks by using a &CFN; template
  title_abbrev: Create a secure bucket that passes default security scanning checks
  synopsis: create a secure bucket that passes default security scanning checks.
  category: CloudFormation templates
  languages:
    CloudFormation YAML:
      versions:
        - sdk_version: 1
          github: S3
          excerpts:
            - description: The YAML version.
              snippet_files:
                - S3/compliant-bucket.yaml
    CloudFormation JSON:
      versions:
        - sdk_version: 1
          github: S3
          excerpts:
            - description: The JSON version (yes, it's actually YAMl right now, I know).
              snippet_files:
                - S3/compliant-bucket.yaml
  services:
    s3: {}
